# Raíz: dependencias para scripts ETL (etl_scripts/)
# Backend (FastAPI) tiene su propio backend/requirements.txt
# Frontend (Next.js) usa package.json
faker>=22.6.0
pandas>=2.1.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
