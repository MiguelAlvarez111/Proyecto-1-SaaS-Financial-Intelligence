# Docs

Add a screenshot of the dashboard as **`preview.png`** in this folder so it appears in the main README on GitHub.

Suggested: capture the Overview page (KPIs + charts) in dark mode, then save as `preview.png`.
